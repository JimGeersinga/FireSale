import { BlobToImagePipe } from './blob-to-image.pipe';

describe('BlobToImagePipe', () => {
  it('create an instance', () => {
    const pipe = new BlobToImagePipe();
    expect(pipe).toBeTruthy();
  });
});
